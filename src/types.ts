export interface SlopeGraphOptions {
  numLines: number;
  leftHeader: string;
  rightHeader: string;
  headerColor: string;
  colorPalette: string;
  invertColorPalette: boolean;
  hoverColor: string;
}
